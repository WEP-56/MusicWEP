import 'package:flutter/material.dart';

/// Stub for non-desktop platforms. Just returns the child unchanged.
Widget buildDesktopWindowFrame({
  required BuildContext context,
  required Widget child,
}) => child;
